{
 :server {:port 8081 :thread 24}
 :front  {
          :camera            {:tile        {:size 64}
                              :scroll-step 1
                              :zoom-step   0.2}
          :keyboard-bindings {:scroll-left  {:key 65}
                              :scroll-right {:key 68}
                              :scroll-top   {:key 87}
                              :scroll-down  {:key 83}
                              :zoom-in      {:key 81}
                              :zoom-out     {:key 69}
                              :console      {:key 192}}}
 }
